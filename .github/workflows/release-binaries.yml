name: release-binaries

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: Existing tag to build and upload assets for (for example v0.2.53)
        required: true
        type: string

permissions:
  contents: write

env:
  ZIG_VERSION: 0.16.0

jobs:
  build:
    name: build-${{ matrix.asset_name }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-15-intel
            zig_target: x86_64-macos
            asset_name: codedb-darwin-x86_64
            zig_archive: zig-x86_64-macos-0.16.0.tar.xz
            zig_dir: zig-x86_64-macos-0.16.0
          - runner: macos-14
            zig_target: aarch64-macos
            asset_name: codedb-darwin-arm64
            zig_archive: zig-aarch64-macos-0.16.0.tar.xz
            zig_dir: zig-aarch64-macos-0.16.0
          - runner: ubuntu-24.04
            zig_target: x86_64-linux
            asset_name: codedb-linux-x86_64
            zig_archive: zig-x86_64-linux-0.16.0.tar.xz
            zig_dir: zig-x86_64-linux-0.16.0
          - runner: ubuntu-24.04
            zig_target: aarch64-linux
            asset_name: codedb-linux-arm64
            zig_archive: zig-x86_64-linux-0.16.0.tar.xz
            zig_dir: zig-x86_64-linux-0.16.0
    env:
      RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.event.release.tag_name }}
      APPLE_CERTIFICATE_P12: ${{ secrets.APPLE_CERTIFICATE_P12 }}
      APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
      CODEDB_CODESIGN_IDENTITY: ${{ secrets.APPLE_CODESIGN_IDENTITY }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ env.RELEASE_TAG }}

      - name: Install Zig
        shell: bash
        run: |
          set -euo pipefail
          curl -fsSL "https://ziglang.org/download/${ZIG_VERSION}/${{ matrix.zig_archive }}" -o zig.tar.xz
          curl -fsSL "https://ziglang.org/download/index.json" -o zig-index.json
          expected_sha="$(python3 -c 'import json, os; data=json.load(open("zig-index.json", "r", encoding="utf-8")); print(data[os.environ["ZIG_VERSION"]][os.environ["ZIG_ARCHIVE"]]["shasum"])')"
          actual_sha="$(shasum -a 256 zig.tar.xz | awk '{print $1}')"
          test "$actual_sha" = "$expected_sha"
          tar -xf zig.tar.xz
          echo "$PWD/${{ matrix.zig_dir }}" >> "$GITHUB_PATH"
        env:
          ZIG_ARCHIVE: ${{ matrix.zig_archive }}

      - name: Import Apple signing certificate
        if: runner.os == 'macOS' && env.APPLE_CERTIFICATE_P12 != ''
        shell: bash
        run: |
          set -euo pipefail
          CERT_PATH="$RUNNER_TEMP/codedb-signing.p12"
          KEYCHAIN_PATH="$RUNNER_TEMP/codedb-signing.keychain-db"
          export CERT_PATH
          python3 - <<'PY'
          import base64
          import os
          import pathlib
          pathlib.Path(os.environ["CERT_PATH"]).write_bytes(
              base64.b64decode(os.environ["APPLE_CERTIFICATE_P12"])
          )
          PY
          security create-keychain -p "" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "" "$KEYCHAIN_PATH"
          security import "$CERT_PATH" -P "$APPLE_CERTIFICATE_PASSWORD" -A -t cert -f pkcs12 -k "$KEYCHAIN_PATH"
          security list-keychains -d user -s "$KEYCHAIN_PATH"
          security default-keychain -s "$KEYCHAIN_PATH"
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "" "$KEYCHAIN_PATH"

      - name: Build ${{ matrix.asset_name }}
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${{ runner.os }}" == "macOS" ]] &&
             [[ "${{ matrix.zig_target }}" != "x86_64-macos" ]] &&
             [[ -n "${CODEDB_CODESIGN_IDENTITY:-}" ]] &&
             grep -q 'codesign-identity' build.zig; then
            zig build -Doptimize=ReleaseFast -Dtarget=${{ matrix.zig_target }} -Dcodesign-identity="$CODEDB_CODESIGN_IDENTITY"
          else
            zig build -Doptimize=ReleaseFast -Dtarget=${{ matrix.zig_target }}
          fi
          cp zig-out/bin/codedb "${{ matrix.asset_name }}"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: ${{ matrix.asset_name }}
          if-no-files-found: error

  publish:
    name: upload-release-assets
    runs-on: ubuntu-24.04
    needs: build
    env:
      RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.event.release.tag_name }}
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Assemble release assets
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p assets
          find dist -type f -name 'codedb-*' -exec cp {} assets/ \;
          (
            cd assets
            sha256sum codedb-* | sort > checksums.sha256
          )

      - name: Upload assets to GitHub Release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh release upload "$RELEASE_TAG" assets/codedb-* assets/checksums.sha256 --clobber --repo "$GITHUB_REPOSITORY"
